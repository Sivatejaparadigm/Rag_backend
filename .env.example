DB_HOST=localhost
DB_PORT=5432
DB_NAME=rag_db
DB_USER=rag_user
DB_PASSWORD=rag_password

# If you prefer a single URL, you can set DATABASE_URL instead (must be URL-encoded if password has special chars)
# DATABASE_URL=postgresql+asyncpg://rag_user:rag_password@localhost:5432/rag_db

SQL_ECHO=false

